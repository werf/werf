name: docs:deploy:v2

on:
  push:
    branches:
      - 2.0

jobs:
  deploy-prod-ru:
    permissions:
      contents: read
      id-token: write
      packages: write
    uses: ./.github/workflows/_docs_deploy_production.yml
    with:
      docsDeployTarget: v2
      docsDeployLatestAliasEnabled: false
      persistent: true
      targetCluster: ru
      runner: prod-github-runner-0
      kubeConfigSecretPath: projects/data/b454e6aa-39f0-45f4-aa7c-a9465ab154cb/KUBE_CONFIG_RU
    secrets: inherit

  deploy-prod-eu:
    permissions:
      contents: read
      id-token: write
      packages: write
    uses: ./.github/workflows/_docs_deploy_production.yml
    with:
      docsDeployTarget: v2
      docsDeployLatestAliasEnabled: false
      persistent: true
      targetCluster: eu
      runner: prod-github-runner-0
      kubeConfigSecretPath: projects/data/b454e6aa-39f0-45f4-aa7c-a9465ab154cb/KUBE_CONFIG
    secrets: inherit

  notify:
    if: always()
    needs:
      - deploy-prod-ru
      - deploy-prod-eu
    uses: werf/common-ci/.github/workflows/notification.yml@main
    secrets:
      loopNotificationGroup: ${{ secrets.LOOP_NOTIFICATION_GROUP }}
      webhook: ${{ secrets.LOOP_NOTIFICATION_WEBHOOK }}
      notificationChannel: ${{ secrets.LOOP_NOTIFICATION_CHANNEL }}



