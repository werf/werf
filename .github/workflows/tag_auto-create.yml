name: tag:auto-create

on:
  push:
    branches:
      - main
      - "[0-9]+.[0-9]+.[0-9]+*"
      - "[0-9]+.[0-9]+"
      - "[0-9]+"
      - "!3"
    paths:
      - CHANGELOG.md
  repository_dispatch:
    types: ["tag:auto-create"]
  workflow_dispatch:

jobs:
  release:
    name: Create release tag
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Relabel merged release PR
        env:
          GH_TOKEN: ${{ secrets.RELEASE_PLEASE_TOKEN }}
          RELEASE_BRANCH: ${{ github.ref_name }}
        run: |
          PR_NUMBER=$(gh pr list --state merged --base "$RELEASE_BRANCH" --label "autorelease: pending" --limit 1 --json number -q '.[0].number // empty')
          if [[ -n "$PR_NUMBER" ]]; then
            gh pr edit "$PR_NUMBER" --remove-label "autorelease: pending" --add-label "autorelease: tagged"
          fi

      - name: Get version from CHANGELOG.md
        id: get_version
        run: |
          VERSION=$(grep -m1 -E '^#+ \[[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?\]' CHANGELOG.md | sed -E 's/^#+ \[([0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?)\].*/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create tag via GitHub API
        env:
          GH_TOKEN: ${{ secrets.RELEASE_PLEASE_TOKEN }}
        run: |
          TAG="v${{ steps.get_version.outputs.version }}"

          if gh api repos/${{ github.repository }}/git/ref/tags/$TAG &>/dev/null; then
            echo "Tag $TAG already exists. Skipping..."
            exit 0
          fi

          COMMIT_SHA=$(git rev-parse HEAD)

          gh api repos/${{ github.repository }}/git/refs \
            -f ref="refs/tags/$TAG" \
            -f sha="$COMMIT_SHA"
