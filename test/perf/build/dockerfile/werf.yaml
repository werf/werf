project: werf-perf-test
configVersion: 1

---
image: neovim
dockerfile: Dockerfile
args:
  INVALIDATE_CACHES: "{{ uuidv4 }}"
  BASE_REGISTRY: {{ env "BASE_REGISTRY" | default "registry.ci.werf.io/base" }}
