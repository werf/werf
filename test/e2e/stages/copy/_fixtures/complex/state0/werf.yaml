project: none
configVersion: 1

---
image: backend
from: python:alpine
import:
  - from: artifact
    add: /artifact
    to: /artifact
    before: setup

---
image: artifact
final: false
from: alpine:3.21
fromCacheVersion: {{ env "ARTIFACT_CACHE_VERSION" }}
shell:
  install:
    - echo {{ env "ARTIFACT_DATA" }} > /artifact
