---
title: Автоматическая очистка хоста
permalink: usage/cleanup/host_cleanup.html
---

## Обзор

Очистка хоста удаляет неактуальных данные и сокращает размер кеша **автоматически** в рамках вызова основных команд werf и сразу **для всех проектов**. При необходимости очистку можно выполнять в ручном режиме с помощью команды [**werf host cleanup**]({{ "reference/cli/werf_host_cleanup.html" | true_relative_url }}).

## Переопределение директории хранилища бэкенда (Docker или Buildah)

Параметр `--backend-storage-path` (или переменная окружения `WERF_BACKEND_STORAGE_PATH`) позволяет явно задать директорию хранилища бэкенда в случае, если werf не может правильно определить её автоматически.

## Изменение порога занимаемого места и глубины очистки хранилища бэкенда (Docker или Buildah)

Параметр `--allowed-backend-storage-volume-usage` (`WERF_ALLOWED_BACKEND_STORAGE_VOLUME_USAGE`) позволяет изменить порог запуска очистки хранилища бэкенда.

- Числа без единиц измерения трактуются как **проценты**. Например, `70` означает, что очистка запускается при превышении `70%` использования хранилища бэкенда.
- Значения с единицами измерения трактуются как **абсолютный порог свободного места**. Например, `10GB` означает, что очистка запускается, если свободное место в хранилище бэкенда опускается ниже `10GB`.

Параметр `--allowed-backend-storage-volume-usage-margin` (`WERF_ALLOWED_BACKEND_STORAGE_VOLUME_USAGE_MARGIN`) задаёт, насколько глубоко должна продолжаться очистка после её запуска.

- В процентном режиме margin тоже задаётся в процентах. Например, `70` и margin `5` означают, что очистка запускается выше `70%` использования и продолжается, пока использование не опустится ниже `65%`.
- В режиме абсолютных значений margin тоже задаётся как абсолютное значение. Например, `10GB` и margin `2GB` означают, что очистка запускается ниже `10GB` свободного места и продолжается, пока свободное место не вырастет до `12GB`.

Если `--allowed-backend-storage-volume-usage` задан с единицами измерения, а `--allowed-backend-storage-volume-usage-margin` явно не задан, werf использует эффективный margin `0B`.

Если оба параметра заданы явно, они должны быть в одном формате:

- `70` и `5` — допустимо;
- `10GB` и `2GB` — допустимо;
- `70` и `2GB` — недопустимо;
- `10GB` и `5` — недопустимо.

## Очистка сборочного кеша сборочного бэкенда (Docker или Buildah)

При очистке хоста werf удаляет контейнеры, образы и неиспользуемые тома, однако стратегия очистки сборочного кэша сборочного бэкенда остаётся на усмотрение пользователя.

К примеру, для бэкенда Docker доступны следующие подходы к очистке:

- Установить порог хранимого кеша в [конфигурации сборщика](https://docs.docker.com/build/cache/garbage-collection/#configuration).
- Настроить регулярную очистку с помощью `cron` и команды `docker buildx prune --max-used-space=bytes`.

По аналогии пользователь может настроить очистку и для Buildah.

## Изменение порога занимаемого места и глубины очистки локального кэша

Параметр `--allowed-local-cache-volume-usage` (`WERF_ALLOWED_LOCAL_CACHE_VOLUME_USAGE`) позволяет изменить порог запуска очистки локального кэша.

- Числа без единиц измерения трактуются как **проценты**. Например, `70` означает, что очистка запускается при превышении `70%` использования локального кэша.
- Значения с единицами измерения трактуются как **абсолютный порог свободного места**. Например, `10GB` означает, что очистка запускается, если свободное место на томе локального кэша опускается ниже `10GB`.

Параметр `--allowed-local-cache-volume-usage-margin` (`WERF_ALLOWED_LOCAL_CACHE_VOLUME_USAGE_MARGIN`) задаёт, насколько глубоко должна продолжаться очистка после её запуска.

- В процентном режиме margin тоже задаётся в процентах. Например, `70` и margin `5` означают, что очистка запускается выше `70%` использования и продолжается, пока использование не опустится ниже `65%`.
- В режиме абсолютных значений margin тоже задаётся как абсолютное значение. Например, `10GB` и margin `2GB` означают, что очистка запускается ниже `10GB` свободного места и продолжается, пока свободное место не вырастет до `12GB`.

Если `--allowed-local-cache-volume-usage` задан с единицами измерения, а `--allowed-local-cache-volume-usage-margin` явно не задан, werf использует эффективный margin `0B`.

Если оба параметра заданы явно, они должны быть в одном формате:

- `70` и `5` — допустимо;
- `10GB` и `2GB` — допустимо;
- `70` и `2GB` — недопустимо;
- `10GB` и `5` — недопустимо.

## Выключение автоматической очистки

Пользователь может выключить автоматическую очистку неактуальных данных хоста с помощью параметра `--disable-auto-host-cleanup` (`WERF_DISABLE_AUTO_HOST_CLEANUP`). В этом случае рекомендуется добавить команду `werf host cleanup` в cron, например, следующим образом:

```shell
# /etc/cron.d/werf-host-cleanup
SHELL=/bin/bash
*/30 * * * * gitlab-runner source ~/.profile ; source $(trdl use werf 2 stable) ; werf host cleanup
```
