.publish:
  script:
    - |
      curl --header "JOB-TOKEN: $CI_JOB_TOKEN" \
        --upload-file "dist/$CI_COMMIT_TAG/linux-amd64/bin/werf" \
        "$CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/werf/$CI_COMMIT_TAG/werf"